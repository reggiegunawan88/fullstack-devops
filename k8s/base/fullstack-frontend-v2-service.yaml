apiVersion: v1
kind: Service
metadata:
  name: fullstack-frontend-v2
  labels:
    app: fullstack-frontend-v2-service
spec:
  selector:
    app: fullstack-frontend-v2
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
