name: Build and Push to ECR

on:
  push:
    branches:
      - master
    paths:
      - "apps/**"
      - ".github/workflows/**"
  pull_request:
    branches:
      - master
    paths:
      - "apps/**"
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: read

    env:
      AWS_REGION: us-east-1

    strategy:
      matrix:
        app:
          - name: backend
            context: ./apps/backend
            dockerfile: ./apps/backend/Dockerfile
            ecr_repo_name: fullstack-devops/backend
          - name: frontend
            context: ./apps/frontend
            dockerfile: ./apps/frontend/Dockerfile
            ecr_repo_name: fullstack-devops/frontend-v1
          - name: frontend-v2
            context: ./apps/frontend-v2
            dockerfile: ./apps/frontend-v2/Dockerfile
            ecr_repo_name: fullstack-devops/frontend-v2
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Configure AWS Credentials with OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Add ECR login step
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Determine next Docker image tag 🐳
        id: ecr-image-tag
        env:
          ECR_REPOSITORY: ${{ matrix.app.ecr_repo_name }}
        run: |
          set -e

          branch="${GITHUB_REF_NAME//\//-}"
          prefix="${branch}-"

          tags=$(aws ecr describe-images \
            --repository-name "${ECR_REPOSITORY}" \
            --query "imageDetails[].imageTags[]" \
            --output text | tr '\t' '\n' | grep "^${prefix}" || true)

          if [[ -z "$tags" ]]; then
            next=1
          else
            max=$(echo "$tags" | sed "s/^${prefix}//" | sort -n | tail -1)
            next=$((max + 1))
          fi

          tag="${prefix}${next}"
          echo "tag=${tag}" >> "$GITHUB_OUTPUT"

      - name: Build & Push to ECR
        uses: docker/build-push-action@v5
        with:
          context: ${{ matrix.app.context }}
          file: ${{ matrix.app.dockerfile }}
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.login-ecr.outputs.registry }}/${{ matrix.app.ecr_repo_name }}:${{ steps.ecr-image-tag.outputs.tag }}

      - name: Output image tag ✅
        run: echo "Pushed image with tag ${{ steps.ecr-image-tag.outputs.tag }}"
